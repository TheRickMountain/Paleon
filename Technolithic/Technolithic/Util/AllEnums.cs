namespace Technolithic
{
    public enum ToolUsageStatus
    {
        Required,
        Optional,
        NotUsed
    }

    public enum InteractionIconDisplayState
    {
        Never,
        OnMarked,
        OnUnmarked
    }
}
